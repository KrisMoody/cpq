# Tasks

## Entity Relationship Visualizations

- [x] Create `EntityDiagram.vue` component with ER-style boxes and relationship lines using SVG
- [x] Create `EntityHierarchy.vue` component with treemap chart showing Product and Quote hierarchies
- [x] Update `learn.vue` to display both visualizations (tabbed)
- [x] Remove existing `EntityRelationshipChart.vue` treemap

## Glossary Examples

- [x] Add `example` prop to `GlossaryTerm.vue` component
- [x] Update glossary data in `learn.vue` with concrete examples for all 12 terms
- [x] Style the example text distinctly (italic with lightbulb icon)

## Validation

- [x] Verify charts render correctly with SSR (ClientOnly wrapper)
- [x] Test glossary search still works with example text included
- [x] Visual review on mobile/tablet breakpoints
