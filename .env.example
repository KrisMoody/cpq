# Database connection string (PostgreSQL)
# For Neon: postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/cpq"

# AI Service Configuration (optional - AI features disabled if not set)
ANTHROPIC_API_KEY=""

# Neon Auth Configuration
# Get this URL from your Neon project dashboard under the Auth tab
# Format: https://<project-id>.<region>.neon.tech/auth
NEON_AUTH_URL=""

# Authentication Bypass (Development Only)
# WARNING: NEVER enable this in production environments!
# When set to "true", bypasses all authentication checks and uses a mock user session.
# This is intended for local development and testing only.
# DISABLE_AUTH="true"
